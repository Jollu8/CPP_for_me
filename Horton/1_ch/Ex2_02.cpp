#include <iostream>
#include <cmath>
using namespace std;

int main()
{
     /*unsigned int yards{}, feet{}, inches{};
    cout << "Enter a distance as yards, feet and inches"
         << "with the three values separated by spaces: ";
    cin >> yards >> feet >> inches;

    const unsigned feet_per_yard{3};
    const unsigned inches_per_foot{12};

    unsigned total_inches{};
    total_inches = inches + inches_per_foot * (yards * feet_per_yard + feet);
    cout << "This distance corresponds to " << total_inches << " inches.\n";

    cout << "Enter a distance in inches: ";
    cin >> total_inches;
    feet = total_inches / inches_per_foot;
    inches = total_inches % inches_per_foot;
    yards = feet % feet_per_yard;
    feet = feet % feet_per_yard;
    cout << "This distance correspond to "
         << yards << " yards"
         << feet << " feet"
         << inches << " inches." << endl;
         return 0;*/
     //     ***************************************************************
     // int height{74};
     // cout << "The height variable occupies " << sizeof height << " bytes." << endl;
     // cout << "Type \" long long\" occupies " << sizeof(long long) << " bytes." << endl;
     // cout << "The result of the expression height * height/2 occupies "
     // << sizeof(height * height / 2) << " bytes." << endl;
     // *******************************************************
     // int count{5};
     // count = count + 1;
     // count += 1;
     // ++count;
     // cout << count << endl;
     // ***************************************************************
     // const double pi{3.141592653589793};
     // double a{0.2};
     // double z{9};
     // double volume{};
     // volume = pi * z * z * a;
     // cout << volume << endl;
     // ***************************************************

     double angle{1.5};
     double cosine_value{cos(angle)};
     const double pi_degress{180};
     double angle_deg{60.0};
     double tangent{tangent};


     return 0;
}